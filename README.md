# MINI_RESUME_ANJANA_K
Mini Resume Management API built using FastAPI. The application allows uploading candidate resumes, storing candidate details in memory, and performing basic CRUD operations such as add, list, view, and delete candidates.
