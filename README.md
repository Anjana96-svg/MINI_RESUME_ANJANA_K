# Mini Resume Management API

## Python Version
Python 3.9

## Setup
pip install -r requirements.txt

## Run
uvicorn app:app --reload

## Endpoints
GET /health  
POST /candidates  
GET /candidates  
GET /candidates/{cid}  
DELETE /candidates/{cid}
